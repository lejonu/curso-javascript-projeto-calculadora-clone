window.calculator = new CalcController();

// Data e hora
// calculator.displayDate = new Date().toLocaleString("pt-BR");

// Somente data
// calculator.displayDate = new Date().toLocaleDateString("pt-BR");

// Somente hora
// calculator.displayDate = new Date().toLocaleTimeString("pt-BR");